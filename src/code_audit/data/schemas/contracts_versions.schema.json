{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "contracts_versions.schema.json",
  "title": "Contract Versions",
  "description": "Single source of truth for versioned contract knobs (signal_logic_version, etc.).",
  "type": "object",
  "required": [
    "signal_logic_version",
    "treesitter_manifest_version",
    "contract_schema_version"
  ],
  "additionalProperties": false,
  "properties": {
    "signal_logic_version": {
      "type": "string",
      "pattern": "^signals_v[0-9]+$",
      "description": "Canonical signal logic version. Bump when golden fixture semantics change."
    },
    "treesitter_manifest_version": {
      "type": "integer",
      "minimum": 1,
      "description": "Treesitter manifest format version."
    },
    "contract_schema_version": {
      "type": "string",
      "pattern": "^contracts_versions_v[0-9]+$",
      "description": "Schema version for this file itself."
    }
  }
}
